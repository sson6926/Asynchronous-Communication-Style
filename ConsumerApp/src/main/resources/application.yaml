spring:
  application:
    name: consumer-app
  main:
    web-application-type: none

  rabbitmq:
    host: ${SPRING_RABBITMQ_HOST:localhost}
    port: 5672
    username: guest
    password: guest

    listener:
      simple:
        acknowledge-mode: auto
        concurrency: 1
        max-concurrency: 1